fastapi>=0.109.0,<1.0
uvicorn[standard]>=0.27.0,<1.0
httpx>=0.26.0,<1.0
beautifulsoup4>=4.12.0
supabase>=2.0.0,<3.0
pydantic>=2.5.0,<3.0
pydantic-settings>=2.1.0,<3.0
python-dotenv>=1.0.0
apscheduler>=3.10.0,<4.0
slowapi>=0.1.9,<1.0
python-jose[cryptography]>=3.3.0
dnspython>=2.4.0
tldextract>=5.1.0
lxml>=5.1.0
python-multipart>=0.0.6
geoip2>=4.8.0,<5.0
# Note: theHarvester is intentionally NOT pinned here.
#   - The `theHarvester` package on PyPI is a broken stub (v0.0.1 only).
#   - The real tool is at github.com/laramies/theHarvester and pulls a
#     heavy dependency tree that conflicts with our pins on cloud builds.
#   - Install locally via your distro (Kali ships it at /usr/bin/theHarvester)
#     or from git in your own venv. modules/harvester.py degrades gracefully
#     when the binary isn't on PATH and the /api/v1/recon/harvest endpoint
#     will return a clear error message in that case.
